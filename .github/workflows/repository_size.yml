name: Repository size

on: [push]

jobs:
    Repository_size:
        name: "Repository size"
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: |
                echo ${{ github.ref.name }}
                du -skh --exclude=".git"